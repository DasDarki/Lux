local arr = {1, 2, 3}

enum Status
    Ok
    Error
    SomethingElse
end

local status = Status.Ok

local msg = match arr
    case Status.Ok then "success"
    case Status.Error then "failure"
    case _ then 1
end